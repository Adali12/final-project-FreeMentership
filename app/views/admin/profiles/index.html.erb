<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <a class="navbar-brand" href="#">FreeMentorship</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item ">
        <%= link_to 'Admin',admin_users_path,class:"nav-link" %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Abot Us </a>
      </li>
      <li class="nav-item ">
        <a class="nav-link" href="#">Contact Us </a>
      </li>
    
      <li class="nav-item active">
        <%=link_to 'Request',admin_profiles_path,class:"nav-link" %>
      </li>
    </ul>
    <%= link_to current_user.lastName,admin_user_path(current_user.id),class:'nav-link ml-auto' %>
    <%= link_to "Logout", session_path(current_user.id), method: :delete ,class:"nav-link"%>
  </div>
</nav>
<div class="container user-cards-container ">
  <p id="notice"><%= notice %></p>
  <div class="card" style="width: 25rem;" >
          <% @profiles.each do |profile| %>
            <div class="card-header" ><H6>Use Email:</h6>
          <%=profile.user.email %>
            </div>
          <div class="card-body" >
          <h2> Resume Cv:
        
         <%= link_to 'Download',profile.cv, download: ''%></h2> 
          </div>
          <div class="card-body" >
          <h2>Image:
          <%= image_tag(profile.pic, style:"width:20%") %></h2>
          </div>
          <div class="card-footer">
         <%= link_to 'Comment', admin_profile_path(profile),class:"btn btn-outline-info" %>
         <%= link_to 'Edit', edit_admin_profile_path(profile),class:"btn btn-outline-info" %>
         <%= link_to 'Destroy',profile, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-outline-danger" %>
</div>
<br>
<% end %>
</div>

</div>